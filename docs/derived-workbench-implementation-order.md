# Derived workbench implementation order

1. Freeze the design and the interactive workflow.
2. Build projective complex witnesses, quasi-isomorphisms, and replacement.
3. Build derived Hom and automatic classical transport.
4. Build tilting complexes and thick-generation witnesses.
5. Build mutation, discovery, complex targets, and edge composition.
6. Build the artifact schema and standalone verifier.
7. Add the command-line program.
8. Move the extension into the Python package and add the workbench.
9. Add parity, fixtures, mutations, determinism, oracle rows, and performance
   evidence at each preceding step.
10. Run every release gate and finish the release notes.
